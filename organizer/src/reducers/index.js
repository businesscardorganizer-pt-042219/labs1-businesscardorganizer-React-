import { /* actions here */} from '../actions';

const initialState = {
    something: []
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer;